// const language = "javascript";
// languageUpperCase = language.toUpperCase();

// let number = 0;
// lastPosition = language.length - 1;

// while (number <= lastPosition) {
//   console.log(languageUpperCase[number]);
//   number++;
// }

const language = "javascript";

for (i = 0; i <= language.length - 1; i++) {
  console.log(language[i].toUpperCase());
}
